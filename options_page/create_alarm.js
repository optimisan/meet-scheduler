function createAlarm(subject) {
  const name = subject.name;
  const times = subject.daysWithTimes;
  const url = subject.meetUrl;
  times.forEach((time, day) => {
    //TODO: Add alarms for current week
  });
}
